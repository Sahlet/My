#include <My/GammaNN.h>

namespace My {

  struct GammaNN_fields {
    std::vector< GammaNN::object > data;
  };

  GammaNN::GammaNN(std::vector< object > data, int units) {

  }

  GammaNN::object GammaNN::operator[](int index) {

  }
}
